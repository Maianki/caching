<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Blog</title>
</head>
<body>
    <h1>Edit a blog</h1>
    <form action="/blogs/<%= blog._id %>?_method=PUT" method="POST">
        <input type="text" name="title" placeholder="blog title" value="<%= blog.title %>">
        <input type="text" name="description" placeholder="blog description" value="<%= blog.description %>">
        <input type="text" name="author" placeholder="author" value="<%= blog.author%>">
        <input type="date" name="createdAt" value="<%= blog.createdAt.toISOString().split('T')[0] %>">
        <p><%= blog.createdAt.toISOString().split('T')[0] %></p>
        <button>Submit</button>
    </form>
</body>
</html>